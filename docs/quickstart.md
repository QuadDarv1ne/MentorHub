# 🚀 MentorHub - Quick Start

## Оба сервера запущены и работают!

### 📍 Адреса:

| Сервис | URL | Описание |
|--------|-----|----------|
| **Backend API** | http://127.0.0.1:8000 | FastAPI REST API |
| **API Docs (Swagger)** | http://127.0.0.1:8000/docs | Интерактивная документация |
| **API Docs (ReDoc)** | http://127.0.0.1:8000/redoc | Альтернативная документация |
| **Frontend** | http://localhost:3001 | Next.js приложение |
| **Health Check** | http://127.0.0.1:8000/health | Проверка состояния |

### ⚡ Быстрый старт

#### Вариант 1: Автоматический запуск (в будущем)
```powershell
.\start-dev.ps1
```

#### Вариант 2: Ручной запуск (текущий)
**Terminal 1 - Backend:**
```powershell
cd backend
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

**Terminal 2 - Frontend:**
```powershell
cd frontend
npm run dev
```

### 🧪 Тестирование API

1. Откройте http://127.0.0.1:8000/docs
2. Попробуйте регистрацию:
   - Endpoint: `POST /api/v1/auth/register`
   - Body:
     ```json
     {
       "email": "test@example.com",
       "password": "Test123!",
       "full_name": "Test User"
     }
     ```
3. Авторизуйтесь:
   - Endpoint: `POST /api/v1/auth/login`
   - Скопируйте `access_token`
   - Нажмите кнопку **Authorize** вверху
4. Тестируйте защищенные endpoints!

### 📱 Тестирование Frontend

1. Откройте http://localhost:3001
2. Попробуйте навигацию по страницам
3. Проверьте регистрацию/авторизацию

### 🛠️ Полезные команды

#### Backend
```powershell
# Создать администратора
cd backend
python scripts/create_admin.py

# Добавить тестовые данные
python scripts/seed_data.py

# Запустить тесты
pytest

# Проверить код
black . --check
flake8
```

#### Frontend
```powershell
cd frontend

# Линтинг
npm run lint

# Форматирование
npm run format

# Тесты
npm test

# Проверка типов
npm run type-check
```

### 📦 База данных

- **Тип**: SQLite (для упрощения разработки)
- **Файл**: `backend/mentorhub.db`
- **Схема**: Создается автоматически при запуске

### 🔧 Конфигурация

#### Backend (.env)
```env
DATABASE_URL=sqlite:///./mentorhub.db
SECRET_KEY=dev-test-secret-key-minimum-32-characters-long-for-jwt-tokens
ENVIRONMENT=development
DEBUG=True
```

#### Frontend (.env.local)
```env
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
```

### 🛑 Остановка серверов

- **Backend**: Нажмите `Ctrl+C` в терминале с uvicorn
- **Frontend**: Нажмите `Ctrl+C` в терминале с npm

### 📚 Документация

- [API Documentation](backend/API.md)
- [Testing Guide](TESTING.md)
- [Deployment Guide](DEPLOYMENT-PRODUCTION.md)
- [Postman Collection](backend/postman_collection.json)

### ✅ Checklist

- [x] Backend запущен на :8000
- [x] Frontend запущен на :3001
- [x] База данных создана
- [x] API Docs доступны
- [ ] Создан тестовый пользователь
- [ ] Создан тестовый ментор
- [ ] Проверена регистрация
- [ ] Проверена авторизация

### 🎯 Следующие шаги

1. Создайте тестового пользователя через `/docs`
2. Изучите доступные endpoints
3. Протестируйте основные функции
4. Откройте frontend и проверьте UI
5. Используйте Postman коллекцию для комплексного тестирования

---

**Удачного тестирования! 🚀**
