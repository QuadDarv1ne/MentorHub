# 🚀 Деплой MentorHub на Amvera

Полное руководство по развёртыванию backend + frontend на платформе Amvera.

## 📋 Архитектура деплоя

```
┌─────────────────────────────────────┐
│         Amvera Container            │
│  ┌───────────────────────────────┐  │
│  │       Supervisor              │  │
│  │  ┌─────────┐  ┌─────────┐   │  │
│  │  │ Backend │  │Frontend │   │  │
│  │  │ :8000   │  │  :3000  │   │  │
│  │  └─────────┘  └─────────┘   │  │
│  └───────────────────────────────┘  │
│           Port 80 (HTTP)            │
└─────────────────────────────────────┘
```

**Компоненты:**
- 🐍 **Backend:** FastAPI на порту 8000
- ⚛️ **Frontend:** Next.js на порту 3000  
- 🔄 **Supervisor:** Управление процессами
- 🌐 **Доступ:** Через порт 80

---

## ⚙️ Настройка переменных окружения

### Обязательные переменные

Зайдите в панель Amvera → Ваш проект → Settings → Environment Variables

#### 1. Database
```bash
DATABASE_URL=postgresql://user:password@host:5432/dbname
```
💡 Создайте PostgreSQL базу данных в Amvera или используйте внешнюю

#### 2. Security
```bash
SECRET_KEY=ваш-очень-длинный-секретный-ключ-минимум-32-символа
```
💡 Сгенерируйте командой: `openssl rand -hex 32`

```bash
ENVIRONMENT=production
```

#### 3. Frontend API URL
```bash
NEXT_PUBLIC_API_URL=https://ваш-домен.amvera.ru
```
💡 Замените на ваш реальный домен Amvera

### Опциональные переменные

#### Email (для уведомлений)
```bash
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ваш-email@gmail.com
SMTP_PASSWORD=ваш-пароль-приложения
EMAIL_SENDER=noreply@mentorhub.com
```

#### Stripe (платежи)
```bash
STRIPE_API_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
```

#### СБП (российские платежи)
```bash
SBP_API_KEY=ваш-sbp-api-key
SBP_MERCHANT_ID=ваш-merchant-id
SBP_WEBHOOK_SECRET=ваш-webhook-secret
```

#### AWS S3 (хранение файлов)
```bash
AWS_ACCESS_KEY_ID=ваш-access-key
AWS_SECRET_ACCESS_KEY=ваш-secret-key
AWS_S3_BUCKET=mentorhub-bucket
AWS_REGION=eu-west-1
```

#### Sentry (мониторинг ошибок)
```bash
SENTRY_DSN=https://...@sentry.io/...
```

#### CORS
```bash
CORS_ORIGINS=https://ваш-домен.amvera.ru,https://www.ваш-домен.amvera.ru
```

---

## 🔧 Пошаговая инструкция деплоя

### Шаг 1: Подготовка проекта

```bash
# Убедитесь, что все изменения закоммичены
git status

# Если есть незакоммиченные изменения
git add .
git commit -m "готово к деплою"
```

### Шаг 2: Push на Amvera

```bash
# Отправка на Amvera
git push amvera main --force
```

### Шаг 3: Настройка переменных окружения

1. Откройте панель Amvera
2. Перейдите в ваш проект
3. Settings → Environment Variables
4. Добавьте все обязательные переменные (см. выше)
5. Сохраните изменения

### Шаг 4: Перезапуск приложения

После добавления переменных Amvera автоматически перезапустит контейнер.

Если нужно перезапустить вручную:
1. Deployments → Latest deployment
2. Кнопка "Restart"

### Шаг 5: Проверка работоспособности

```bash
# Проверка health endpoint
curl https://ваш-домен.amvera.ru/api/v1/health

# Ожидаемый ответ:
{
  "status": "healthy",
  "service": "MentorHub API",
  "version": "1.0.0",
  "environment": "production",
  "database": "connected"
}
```

---

## 🐛 Troubleshooting

### Проблема: Container не запускается

**Решение:**
1. Проверьте логи в Amvera → Logs
2. Убедитесь, что все обязательные переменные установлены
3. Проверьте формат `DATABASE_URL`

### Проблема: Frontend не работает

**Решение:**
1. Проверьте, что `NEXT_PUBLIC_API_URL` правильный
2. Посмотрите логи frontend: `/var/log/frontend.out.log`
3. Убедитесь, что supervisor запустил оба процесса

### Проблема: Backend работает, но API не отвечает

**Решение:**
```bash
# Проверьте логи backend
cat /var/log/backend.out.log

# Проверьте, слушает ли backend на 8000
netstat -tlnp | grep 8000
```

### Проблема: Database connection error

**Решение:**
1. Проверьте формат `DATABASE_URL`:
   ```
   postgresql://username:password@host:5432/database
   ```
2. Убедитесь, что база данных создана
3. Проверьте права доступа

### Проблема: Build failed

**Решение:**
1. Проверьте, что `requirements.txt` существует
2. Проверьте, что `package.json` корректный
3. Посмотрите полный лог сборки в Amvera

---

## 📊 Мониторинг

### Логи приложения

**Backend логи:**
```bash
tail -f /var/log/backend.out.log
tail -f /var/log/backend.err.log
```

**Frontend логи:**
```bash
tail -f /var/log/frontend.out.log
tail -f /var/log/frontend.err.log
```

**Supervisor логи:**
```bash
tail -f /var/log/supervisor/supervisord.log
```

### Health checks

```bash
# Backend health
curl http://localhost:8000/health

# Frontend health (должна открываться главная страница)
curl http://localhost:3000
```

---

## 🔒 Безопасность

### Обязательно перед production:

- [ ] Установите надёжный `SECRET_KEY` (32+ символа)
- [ ] Настройте `CORS_ORIGINS` только для вашего домена
- [ ] Используйте `ENVIRONMENT=production`
- [ ] Включите HTTPS в настройках Amvera
- [ ] Настройте SSL сертификат
- [ ] Установите strong пароль для базы данных
- [ ] Регулярно обновляйте зависимости

---

## 📈 Производительность

### Рекомендуемые настройки Amvera

```yaml
resources:
  memory: 2048  # 2GB RAM
  cpu: 1.0      # 1 vCPU
```

Для больших нагрузок увеличьте до:
```yaml
resources:
  memory: 4096  # 4GB RAM
  cpu: 2.0      # 2 vCPU
```

---

## 🔄 Обновление приложения

```bash
# 1. Внесите изменения в код
git add .
git commit -m "описание изменений"

# 2. Push на GitHub
git push origin main

# 3. Push на Amvera (автоматический rebuild)
git push amvera main

# Amvera автоматически:
# - Соберёт новый Docker image
# - Остановит старый контейнер
# - Запустит новый контейнер
```

---

## 📚 Полезные команды

```bash
# Проверка статуса supervisor
supervisorctl status

# Перезапуск backend
supervisorctl restart backend

# Перезапуск frontend
supervisorctl restart frontend

# Перезапуск всех сервисов
supervisorctl restart all

# Просмотр конфигурации
cat /etc/supervisor/conf.d/supervisord.conf
```

---

## 🆘 Поддержка

Если возникли проблемы:

1. **Проверьте документацию:** [docs.amvera.ru](https://docs.amvera.ru)
2. **Логи Amvera:** Deployments → Latest → Logs
3. **GitHub Issues:** [github.com/QuadDarv1ne/MentorHub/issues](https://github.com/QuadDarv1ne/MentorHub/issues)

---

## ✅ Checklist деплоя

- [ ] Код закоммичен в Git
- [ ] Push на Amvera выполнен
- [ ] Все обязательные env variables установлены
- [ ] Database создана и доступна
- [ ] Health check возвращает 200 OK
- [ ] Frontend открывается в браузере
- [ ] Backend API отвечает на запросы
- [ ] HTTPS настроен
- [ ] Логи проверены на ошибки
- [ ] Мониторинг настроен (опционально)

---

**Готово! 🎉**

Ваше приложение MentorHub теперь запущено на Amvera с backend и frontend!

**URL:** `https://ваш-домен.amvera.ru`
