# 🚀 Развертывание MentorHub на Amvera

Это руководство поможет вам развернуть full-stack приложение MentorHub на платформе [Amvera](https://amvera.ru).

## 📋 Содержание

- [Предварительные требования](#предварительные-требования)
- [Подготовка проекта](#подготовка-проекта)
- [Регистрация на Amvera](#регистрация-на-amvera)
- [Создание проекта](#создание-проекта)
- [Настройка переменных окружения](#настройка-переменных-окружения)
- [Деплой приложения](#деплой-приложения)
- [Настройка домена](#настройка-домена)
- [Мониторинг и логи](#мониторинг-и-логи)
- [Устранение неполадок](#устранение-неполадок)

---

## 🔧 Предварительные требования

- Аккаунт на [GitHub](https://github.com)
- Ваш проект должен быть загружен в GitHub репозиторий
- Аккаунт на [Amvera.ru](https://amvera.ru) (регистрация бесплатная)

---

## 📦 Подготовка проекта

### 1. Убедитесь, что у вас есть все необходимые файлы:

```
MentorHub/
├── amvera.yml                    # ✅ Конфигурация Amvera
├── Dockerfile.production         # ✅ Production Dockerfile
├── docker-entrypoint.sh          # ✅ Скрипт запуска
├── .dockerignore                 # ✅ Исключения для Docker
├── backend/
│   ├── .dockerignore
│   ├── requirements.txt
│   └── ...
└── frontend/
    ├── .dockerignore
    ├── package.json
    └── ...
```

### 2. Проверьте локально (опционально):

```bash
# Сборка production образа
docker build -f Dockerfile.production -t mentorhub:latest .

# Запуск контейнера
docker run -p 8000:8000 mentorhub:latest
```

---

## 🌐 Регистрация на Amvera

### Шаг 1: Создайте аккаунт

1. Перейдите на [amvera.ru](https://amvera.ru)
2. Нажмите **"Войти"** или **"Регистрация"**
3. Зарегистрируйтесь через:
   - GitHub (рекомендуется)
   - Email
   - VK

### Шаг 2: Подключите GitHub

1. В личном кабинете перейдите в **"Настройки"**
2. Найдите раздел **"Интеграции"**
3. Нажмите **"Подключить GitHub"**
4. Авторизуйте Amvera доступ к вашим репозиториям

---

## 🏗️ Создание проекта

### Шаг 1: Создание нового проекта

1. В панели управления нажмите **"+ Новый проект"**
2. Выберите **"Из GitHub репозитория"**
3. Найдите и выберите **MentorHub**
4. Выберите ветку: **main** (или **master**)

### Шаг 2: Настройка проекта

**Тип приложения:**
- ✅ **Docker** (Amvera автоматически определит `Dockerfile.production`)

**Порт приложения:**
- `8000` (backend FastAPI)

**Команда запуска:**
- Оставьте пустым (используется `CMD` из Dockerfile)

---

## ⚙️ Настройка переменных окружения

В панели проекта перейдите в **"Переменные окружения"** и добавьте:

### 🔒 Обязательные переменные

```bash
# Security (сгенерируйте секретный ключ)
SECRET_KEY=ваш-супер-секретный-ключ-минимум-32-символа

# Database (автоматически создается Amvera)
# DATABASE_URL будет добавлен автоматически при подключении PostgreSQL
# REDIS_URL будет добавлен автоматически при подключении Redis
```

### 📧 Email настройки (для уведомлений)

```bash
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_SENDER=noreply@mentorhub.com
```

**Как получить App Password для Gmail:**
1. Перейдите в [Google Account Security](https://myaccount.google.com/security)
2. Включите 2FA (двухфакторную аутентификацию)
3. Создайте **App Password**
4. Используйте сгенерированный пароль в `SMTP_PASSWORD`

### 💳 Платежные системы (опционально)

```bash
# Stripe
STRIPE_API_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
```

### 🎥 Agora (для видеозвонков, опционально)

```bash
AGORA_APP_ID=ваш-app-id
AGORA_APP_CERTIFICATE=ваш-сертификат
```

### ☁️ AWS S3 (для загрузки файлов, опционально)

```bash
AWS_ACCESS_KEY_ID=ваш-aws-key
AWS_SECRET_ACCESS_KEY=ваш-aws-secret
AWS_S3_BUCKET=mentorhub-bucket
```

---

## 🗄️ Подключение сервисов

### PostgreSQL

1. В проекте нажмите **"+ Добавить сервис"**
2. Выберите **PostgreSQL**
3. Версия: **16** (или последняя)
4. Размер хранилища: **1 GB** (для начала)
5. Нажмите **"Создать"**

Amvera автоматически добавит переменную `DATABASE_URL`

### Redis

1. Нажмите **"+ Добавить сервис"**
2. Выберите **Redis**
3. Версия: **7**
4. Нажмите **"Создать"**

Amvera автоматически добавит переменную `REDIS_URL`

---

## 🚀 Деплой приложения

### Автоматический деплой

1. В настройках проекта включите **"Автоматический деплой"**
2. Выберите ветку: **main**
3. Теперь при каждом `git push` в `main` будет автоматический деплой

### Ручной деплой

1. В панели проекта нажмите **"Развернуть"**
2. Выберите коммит или ветку
3. Нажмите **"Начать развертывание"**

### Процесс деплоя

Amvera выполнит:
1. ✅ Клонирование репозитория
2. ✅ Сборка Docker образа
3. ✅ Запуск миграций БД (через `docker-entrypoint.sh`)
4. ✅ Запуск приложения
5. ✅ Health check

**Время деплоя:** ~5-10 минут (первый раз), ~2-3 минуты (последующие)

---

## 🌍 Настройка домена

### Использование поддомена Amvera

По умолчанию Amvera выдаст вам URL вида:
```
https://mentorhub-abc123.amvera.app
```

### Подключение своего домена

1. В настройках проекта перейдите в **"Домены"**
2. Нажмите **"+ Добавить домен"**
3. Введите ваш домен: `mentorhub.ru`
4. Добавьте DNS записи в вашем регистраторе:

```
Type: CNAME
Name: @
Value: mentorhub-abc123.amvera.app
TTL: 3600
```

5. Подождите распространения DNS (до 24 часов)
6. SSL сертификат будет выпущен автоматически (Let's Encrypt)

---

## 📊 Мониторинг и логи

### Просмотр логов

1. Перейдите в **"Логи"** в панели проекта
2. Выберите тип логов:
   - **Application** (логи приложения)
   - **Build** (логи сборки)
   - **System** (системные логи)

### Метрики

В разделе **"Метрики"** вы увидите:
- 📈 CPU использование
- 💾 Memory использование
- 🌐 Network трафик
- 📦 Disk использование

### Health Check

Amvera автоматически проверяет здоровье приложения:
```
GET http://your-app.amvera.app/api/v1/health
```

Если ваш backend не имеет этого endpoint, добавьте его:

```python
# backend/app/api/health.py
from fastapi import APIRouter

router = APIRouter()

@router.get("/health")
async def health_check():
    return {
        "status": "healthy",
        "service": "MentorHub API",
        "version": "1.0.0"
    }
```

---

## 🔧 Устранение неполадок

### Проблема 1: Сборка падает с ошибкой

**Решение:**
1. Проверьте логи сборки в разделе **"Build Logs"**
2. Убедитесь, что `Dockerfile.production` корректный
3. Проверьте `.dockerignore` файлы

### Проблема 2: Приложение не запускается

**Решение:**
1. Проверьте логи приложения
2. Убедитесь, что все переменные окружения заданы
3. Проверьте, что PostgreSQL и Redis подключены
4. Проверьте health check endpoint

### Проблема 3: База данных не доступна

**Решение:**
1. Проверьте, что PostgreSQL сервис запущен
2. Убедитесь, что `DATABASE_URL` правильно установлен
3. Проверьте миграции: `alembic upgrade head`

### Проблема 4: Медленная работа

**Решение:**
1. Увеличьте ресурсы в настройках проекта:
   - CPU: 1 core
   - Memory: 1 GB
2. Оптимизируйте Docker образ (многоступенчатая сборка уже применена)
3. Включите Redis кеширование

### Проблема 5: Ошибки CORS

**Решение:**
Добавьте домен Amvera в CORS настройки backend:

```python
# backend/app/main.py
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://mentorhub-abc123.amvera.app",
        "https://mentorhub.ru",  # ваш домен
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

---

## 📝 Полезные команды

### Локальное тестирование

```bash
# Сборка production образа
docker build -f Dockerfile.production -t mentorhub:prod .

# Запуск с переменными окружения
docker run -p 8000:8000 \
  -e DATABASE_URL="postgresql://user:pass@localhost/db" \
  -e REDIS_URL="redis://localhost:6379" \
  -e SECRET_KEY="test-secret-key" \
  mentorhub:prod

# Проверка здоровья
curl http://localhost:8000/api/v1/health
```

### Git workflow для деплоя

```bash
# Внесите изменения
git add .
git commit -m "feat: добавил новую функцию"

# Отправьте в main для автодеплоя
git push origin main

# Или создайте тег для релиза
git tag -a v1.0.0 -m "Release v1.0.0"
git push origin v1.0.0
```

---

## 🎯 Тарифы Amvera

### Бесплатный тариф (для тестирования)

- ✅ 0.5 CPU
- ✅ 512 MB RAM
- ✅ 1 GB диск
- ✅ PostgreSQL (100 MB)
- ✅ Redis (50 MB)
- ⚠️ Спящий режим после 30 мин неактивности

### Платные тарифы (для production)

**Стартовый (~500₽/мес):**
- 1 CPU
- 1 GB RAM
- 10 GB диск
- PostgreSQL (1 GB)
- Redis (256 MB)

**Профессиональный (~1500₽/мес):**
- 2 CPU
- 2 GB RAM
- 50 GB диск
- PostgreSQL (5 GB)
- Redis (1 GB)
- Автоскейлинг

---

## 🆘 Поддержка

**Документация Amvera:**
- https://docs.amvera.ru

**MentorHub Issues:**
- https://github.com/QuadDarv1ne/MentorHub/issues

**Контакты:**
- Email: maksimqwe42@mail.ru
- Telegram: @quadd4rv1n7

---

## ✅ Checklist перед деплоем

- [ ] Все файлы конфигурации созданы
- [ ] Переменные окружения настроены
- [ ] PostgreSQL сервис подключен
- [ ] Redis сервис подключен
- [ ] Health check endpoint работает
- [ ] CORS настроен правильно
- [ ] Email SMTP настроен (опционально)
- [ ] Домен настроен (опционально)
- [ ] SSL сертификат активен

---

**Поздравляем! 🎉 Ваш MentorHub успешно развернут на Amvera!**

*Последнее обновление: 24.11.2025*
