# Система аутентификации MentorHub

## Обзор

Реализована полноценная система аутентификации с защитой маршрутов, валидацией форм и улучшенным UX.

## Основные компоненты

### 1. useAuth Hook (`lib/hooks/useAuth.ts`)

Централизованный хук для управления аутентификацией:

**Функции:**
- `isAuthenticated()` - проверка авторизации пользователя
- `login(token, userData)` - вход в систему с сохранением токена и данных
- `logout()` - выход из системы с очисткой данных
- `getUserData()` - получение данных текущего пользователя

**Защита маршрутов:**
- Автоматическое перенаправление неавторизованных пользователей на `/auth/login?redirect=`
- Список защищенных маршрутов:
  - `/dashboard`
  - `/profile`
  - `/settings`
  - `/messages`
  - `/notifications`
  - `/sessions`
  - `/booking`
  - `/learning`
  - `/stats`
  - `/achievements`
  - `/billing`
  - `/payment`

### 2. Страница входа (`app/auth/login/page.tsx`)

**Функционал:**
- ✅ Валидация email и пароля на стороне клиента
- ✅ Показ/скрытие пароля (Eye/EyeOff иконки)
- ✅ Состояния загрузки с анимацией
- ✅ Уведомления об ошибках и успехе
- ✅ Чекбокс "Запомнить меня"
- ✅ Mock авторизация (генерация JWT токена)
- ✅ Поддержка redirect параметра (`?redirect=/profile`)
- ✅ Кнопки социального входа (Google, GitHub)
- ✅ Демо-режим с подсказками

**Валидация:**
- Email должен содержать @
- Пароль минимум 6 символов
- Все поля обязательны

### 3. Страница регистрации (`app/auth/register/page.tsx`)

**Функционал:**
- ✅ Расширенная валидация
- ✅ Индикатор силы пароля (5 уровней)
- ✅ Показ/скрытие пароля для обоих полей
- ✅ Визуальное подтверждение совпадения паролей
- ✅ Чекбокс согласия с условиями
- ✅ Mock регистрация с автоматическим входом
- ✅ Кнопки социальной регистрации
- ✅ Состояния загрузки и уведомления

**Валидация:**
- Email корректного формата
- Имя пользователя минимум 3 символа
- Пароль минимум 6 символов
- Пароли должны совпадать
- Обязательное согласие с условиями

**Индикатор силы пароля:**
- Слабый (красный) - до 2 баллов
- Средний (желтый) - 3 балла
- Сильный (зеленый) - 4-5 баллов

Критерии:
- Длина 6+ символов
- Длина 10+ символов
- Заглавные и строчные буквы
- Цифры
- Спецсимволы

### 4. Восстановление пароля (`app/auth/forgot-password/page.tsx`)

**Функционал:**
- ✅ Валидация email
- ✅ Mock отправка email с инструкциями
- ✅ Двухэтапный UI (ввод email → подтверждение отправки)
- ✅ Возможность повторной отправки
- ✅ Информационные блоки с инструкциями
- ✅ Демо-режим с пояснениями

**Процесс восстановления:**
1. Пользователь вводит email
2. Система показывает подтверждение отправки
3. В production - отправка email с токеном
4. Пользователь переходит по ссылке
5. Создает новый пароль

### 5. Защита профиля (`app/profile/page.tsx`)

**Изменения:**
- ✅ Интеграция useAuth hook
- ✅ Загрузка данных пользователя из localStorage
- ✅ Экран загрузки при проверке авторизации
- ✅ Автоматическое перенаправление неавторизованных пользователей

## Хранение данных

### LocalStorage ключи:
- `access_token` - JWT токен пользователя
- `user_data` - JSON объект с данными (email, name, id)
- `user_name` - имя пользователя для быстрого доступа

### Пример данных:
```json
{
  "email": "user@example.com",
  "name": "username",
  "id": 1234567890
}
```

## Демо-режим

Все страницы работают в демо-режиме:
- **Логин:** Принимает любой email + пароль 6+ символов
- **Регистрация:** Создает mock аккаунт без сохранения на сервере
- **Восстановление:** Имитирует отправку email

### Демо-учетные данные:
```
Email: test@example.com
Password: password123
```

## Дизайн и UX

### Общие элементы:
- 🎨 Градиентные фоны (indigo, purple, blue оттенки)
- 🎯 Иконки от lucide-react
- ✨ Плавные анимации и переходы
- 📱 Адаптивный дизайн
- ⚡ Состояния загрузки с спиннерами
- 🔔 Цветные уведомления (красные - ошибки, зеленые - успех, желтые - демо)

### Цветовая схема:
- Основной: Indigo 600
- Успех: Green 600
- Ошибка: Red 600
- Предупреждение: Yellow 600

## Интеграция с backend (TODO)

Для production версии необходимо:

### 1. Login API
```typescript
POST /api/auth/login
{
  "email": "string",
  "password": "string"
}

Response:
{
  "access_token": "jwt_token",
  "user": {
    "id": number,
    "email": "string",
    "name": "string"
  }
}
```

### 2. Register API
```typescript
POST /api/auth/register
{
  "email": "string",
  "username": "string",
  "password": "string"
}

Response: аналогично login
```

### 3. Password Reset API
```typescript
POST /api/auth/forgot-password
{
  "email": "string"
}

Response:
{
  "message": "Email sent"
}
```

### 4. Token Validation
```typescript
GET /api/auth/me
Headers: Authorization: Bearer {token}

Response:
{
  "id": number,
  "email": "string",
  "name": "string"
}
```

## Безопасность

### Текущие меры:
- ✅ Клиентская валидация форм
- ✅ Минимальные требования к паролю
- ✅ Показатель силы пароля
- ✅ HTTPS предполагается в production

### TODO для production:
- [ ] Server-side валидация
- [ ] Rate limiting для API
- [ ] CSRF токены
- [ ] Refresh tokens
- [ ] Двухфакторная аутентификация
- [ ] Хеширование паролей (bcrypt)
- [ ] Защита от brute-force атак
- [ ] Email верификация
- [ ] Логирование попыток входа

## Тестирование

### Сценарии для тестирования:

1. **Вход существующего пользователя**
   - Перейти на `/auth/login`
   - Ввести email и пароль
   - Нажать "Войти"
   - Проверить перенаправление на `/dashboard`

2. **Регистрация нового пользователя**
   - Перейти на `/auth/register`
   - Заполнить все поля
   - Проверить индикатор силы пароля
   - Согласиться с условиями
   - Нажать "Зарегистрироваться"
   - Проверить автоматический вход

3. **Восстановление пароля**
   - Перейти на `/auth/forgot-password`
   - Ввести email
   - Проверить экран подтверждения
   - Попробовать повторную отправку

4. **Защита маршрутов**
   - Очистить localStorage
   - Попытаться открыть `/profile`
   - Проверить перенаправление на `/auth/login?redirect=/profile`
   - Войти в систему
   - Проверить возврат на `/profile`

5. **Выход из системы**
   - Авторизоваться
   - Нажать "Выйти" в Header
   - Проверить очистку localStorage
   - Проверить перенаправление на главную

## Файловая структура

```
frontend/
├── lib/
│   └── hooks/
│       └── useAuth.ts              # Централизованная логика аутентификации
├── app/
│   ├── auth/
│   │   ├── login/
│   │   │   └── page.tsx           # Страница входа
│   │   ├── register/
│   │   │   └── page.tsx           # Страница регистрации
│   │   └── forgot-password/
│   │       └── page.tsx           # Восстановление пароля
│   └── profile/
│       └── page.tsx               # Защищенная страница профиля
└── components/
    └── Header.tsx                 # Проверка авторизации, кнопка выхода
```

## Обновления (22.11.2024)

### Исправлена критическая проблема безопасности:
- ❌ **Было:** Профиль и другие защищенные страницы были доступны без авторизации
- ✅ **Стало:** Все защищенные маршруты проверяют авторизацию и перенаправляют на логин

### Добавлено:
- Централизованный хук `useAuth` для управления аутентификацией
- Полная валидация форм с user-friendly сообщениями об ошибках
- Индикатор силы пароля при регистрации
- Страница восстановления пароля
- Состояния загрузки и анимации
- Демо-режим для всех страниц авторизации

### Улучшено:
- UX страниц логина и регистрации
- Визуальная обратная связь (уведомления, спиннеры)
- Дизайн с градиентами и современными компонентами
- Типизация TypeScript
- Обработка ошибок

## Поддержка

При возникновении вопросов или проблем с аутентификацией:
1. Проверьте localStorage в DevTools
2. Проверьте консоль браузера на наличие ошибок
3. Убедитесь, что используете корректный формат email
4. Проверьте минимальную длину пароля (6 символов)

---

**Статус:** ✅ Базовая система готова для демо  
**Backend интеграция:** ⏸️ В ожидании  
**Email отправка:** ⏸️ В ожидании
